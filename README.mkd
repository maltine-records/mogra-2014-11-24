
# Development

```
npm install grunt bower -g
npm install
bower install
grunt
```

open http://localhost:3000/

edit `src` direcotry

# Release

```
grunt make
```

copy `dest` direcotry to public_html folder
